#include <stdio.h>

int main() {
    
    int even = 0, values;

    for (int i = 0; i < 5; i++) {
        scanf("%d", &values);
        if (values % 2 == 0) {
            even++;
    }
    }
    printf("%d valores pares\n", even);

        return 0;
}
