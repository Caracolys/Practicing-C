#include <stdio.h>

int main() {
    int numb, count = 0, i;

    for (i = 0; i < 6; i++) {
        scanf("%d", &numb);
        if (numb > 0) {
            count++;
        }
    }

    printf("%d valores positivos\n", count);
    return 0;
}
